apiVersion: kargo.akuity.io/v1alpha1
kind: ClusterPromotionTask
metadata:
  name: helmchart-https
spec:
  vars:
  - name: gitopsRepo
  - name: appName
  - name: appPath
  - name: chartPath
    value: "helmchart.yaml"
  - name: chartUrl
  - name: chartName
  - name: warehouse
  - name: versionPrefix
    value: " "
  steps:
  - uses: git-clone
    config:
      repoURL: ${{ vars.gitopsRepo }}
      checkout:
      - branch: main
        path: ./src
      - branch: kargo-${{ vars.appName }}
        path: ./out
        create: true
  # Clean the stage branch of the service
  - uses: git-clear
    config:
      path: ./out
  - uses: copy
    config:
      inPath: ./src/${{ vars.appPath }}
      outPath: ./out/${{ vars.appPath }}
  - uses: copy
    config:
      inPath: ./src/ext
      outPath: ./out/ext
  - uses: yaml-update
    as: update
    config:
      path: ./out/${{ vars.appPath }}/${{ vars.chartPath }}
      updates:
      - key: "spec.version"
        value: ${{ vars.versionPrefix }}${{ chartFrom(vars.chartUrl, vars.chartName, warehouse(vars.warehouse)).Version }}
  - uses: git-commit
    as: commit
    config:
      path: ./out
      message: ${{ task.outputs.update.commitMessage }}
  - uses: git-push
    config:
      path: ./out
  - uses: argocd-update
    config:
      apps:
      - name: ${{ vars.appName }}
        sources:
        - repoURL: ${{ vars.gitopsRepo }}
          desiredRevision: ${{ task.outputs.commit.commit }}
