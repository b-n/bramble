apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dex
  namespace: dex
spec:
  secretName: dex-tls
  dnsNames:
  - "dex.bennaylor.dev"
  issuerRef:
    name: acme
    kind: ClusterIssuer
