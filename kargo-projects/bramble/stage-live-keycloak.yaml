apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: live-keycloak
spec:
  vars:
  - name: appName
    value: bramble-keycloak
  - name: appPath
    value: keycloak
  - name: gitopsRepo
    value: https://github.com/b-n/bramble.git
  promotionTemplate:
    spec:
      steps:
      - task:
          name: containers
          kind: ClusterPromotionTask
  requestedFreight:
  - origin:
      kind: Warehouse
      name: keycloak
    sources:
      direct: true
