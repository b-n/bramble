apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak
spec:
  secretName: keycloak-tls
  dnsNames:
  - "keycloak.bennaylor.dev"
  issuerRef:
    name: acme
    kind: ClusterIssuer
